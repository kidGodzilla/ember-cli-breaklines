import {
  breakLines
} from '../../../helpers/break-lines';
import { module, test } from 'qunit';

module('BreakLinesHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = breakLines(42);
  assert.ok(result);
});
